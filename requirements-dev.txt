# Offline/eval/ingestion tooling — NOT installed in the serving container.
# Keeps the API image slim (see DECISIONS.md, L7 / torch-removal). Install
# both files for local dev / running scripts / evaluation / tests:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

# Testing
pytest==8.4.2
pytest-asyncio==0.25.3

# Evaluation (scripts/evaluation/*, src/agents/eval_agent.py secondary metrics)
rouge-score==0.1.2
bert-score==0.3.13

# Ingestion (scripts/ingestion/*)
pandas==2.2.3
beautifulsoup4==4.12.3
feedparser==6.0.11
google-api-python-client==2.154.0
requests==2.32.3
